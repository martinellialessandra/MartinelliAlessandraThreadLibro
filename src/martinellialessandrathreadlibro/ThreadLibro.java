/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
//package martinellialessandrathreadlibro;
//import javax.swing.*;
//import java.util.Random;

// Classe che rappresenta il thread di ogni libr
//public class ThreadLibro extends Thread {
   // private Libro libro;
   // private JProgressBar progressBar;

    //private Random random = new Random();

    //public ThreadLibro(Libro libro, JProgressBar progressBar) {
        //this.libro = libro;
        //this.progressBar = progressBar;

        // Imposta colore e titolo della progress bar
        //progressBar.setForeground(libro.getColoreCopertina());
        //progressBar.setStringPainted(true);
        //progressBar.setString(libro.getTitolo());
    //}

    //@Override
    //public void run() {
       // int value = 0;
       // while (value < 100) {
            //try {
               // Thread.sleep(random.nextInt(200)); // Simula tempo di lettura
                //value += random.nextInt(10);       // Incremento casuale
                //if (value > 100) value = 100;
               // progressBar.setValue(value);
            //} catch (InterruptedException e) {
               // e.printStackTrace();
           // }
      //  }
   // }
    //
//}


